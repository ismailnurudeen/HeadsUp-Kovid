import 'package:flutter/material.dart';

class QuestionController extends StatelessWidget {
  const QuestionController({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
